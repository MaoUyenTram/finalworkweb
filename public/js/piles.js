$(function () {

    /*$('.addpile').on('click', function () {
        let _token = $('meta[name="csrf-token"]').attr('content');
        let cId = $('[name=cId]').val();
        let id = $('[name=id]').val();
        let name = $('[name=name]').val();
        let amount = $('[name=amount]').val();
        $.ajax({
            url: "/items/store",
            type: "PUT",
            data: {
                name: name,
                amount: amount,
                cId: cId,
                id: id,
                _token: _token
            },
            success: function (response) {
                console.log(response);
            },
        });

    });*/
});
